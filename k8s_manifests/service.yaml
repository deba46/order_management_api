apiVersion: v1
kind: Service
metadata:
  name: order-api-service
  namespace: orders-api
  labels:
    app: order-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: order-api
